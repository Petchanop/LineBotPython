{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd lineBot && python manage.py migrate && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd lineBot && uvicorn lineBot.asgi:app --host 0.0.0.0 --port $PORT"
  }
}